/** @type {import('next').NextConfig} */
const nextConfig = {
    // This Next.js app now serves as a landing/documentation page.
    // The core extension logic is in the /public directory.
    output: 'export',
    distDir: 'out',
};

export default nextConfig;
